db:
  image: mysql

  volumes:
    - /data/mysql:/var/lib/mysql

redis:
  image: redis

  volumes:
    - /data/redis:/var/lib/redis

web:
  build: .
  command: python app.py
  links:
    - db
    - redis
  ports:
    - "8080:5000"
