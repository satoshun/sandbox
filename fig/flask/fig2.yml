db:
  image: tutum/mysql:5.6

initdb:
  build: /data/mysql
  link:
    - db:db
  command: /usr/local/bin/init_db

redis:
  image: redis

web:
  build: .
  command: python app.py
  links:
    - db
    - redis
  wait:
    - initdb
  ports:
    - "8000:8000"
